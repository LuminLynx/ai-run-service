apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-run-service.fullname" . }}
  labels:
    {{- include "ai-run-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-run-service.selectorLabels" . | nindent 4 }}
